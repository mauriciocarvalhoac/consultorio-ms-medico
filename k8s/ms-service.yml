apiVersion: v1
kind: Service
metadata:
  name: consultorio-ms-medico
spec:
  type: NodePort
  ports:
    - port: 80 # Porta interna
      targetPort: 8080 # Porta do Container do Pod
      nodePort: 30080 # Porta externa do Node
  selector:
    app: consultorio-ms-medico
